[pytest]
testpaths = tests
addopts = -ra
filterwarnings =
    ignore:websockets\.legacy is deprecated.*:DeprecationWarning:websockets\.legacy
markers =
    functional: functional smart contract tests
    negative: negative and access-control tests
    security: basic security validation tests
    gas: gas usage validation tests

